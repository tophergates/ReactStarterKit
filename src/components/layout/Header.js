import React from 'react';

import NavBar from './NavBar';

export default () => {
  return (
    <header>
      <h1>Start Kit</h1>
      <h2>React + Redux + Webpack + Babel</h2>
      <NavBar />
    </header>
  )
}
